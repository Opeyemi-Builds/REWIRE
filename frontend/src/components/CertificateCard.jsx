export default function CertificateCard({ title }) {
  return (
    <div style={{ padding: '1rem', borderRadius: '12px', border: '1px solid #e5e7eb' }}>
      <h3>{title}</h3>
      <p>Certificate earned</p>
    </div>
  );
}
