# AI Module

This folder is reserved for AI prompts, recommendation services, and model adapters.

The current implementation includes a mock recommendation layer that works without external credentials so development can continue safely.
