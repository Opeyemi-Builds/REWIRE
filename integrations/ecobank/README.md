# Ecobank Integration

This folder contains any Ecobank-specific adapter or integration logic that should stay separate from the main backend service layer.

The app currently uses the backend service implementation in `backend/services/ecobankService.js`, while this directory is kept ready for more explicit adapter work or future abstraction.
